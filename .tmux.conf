#
# Example .tmux.conf
#
# By Nicholas Marriott. Public domain.
#

# Some tweaks to the status line
set -g status-right "%H:%M"
set -g window-status-current-attr "underscore"

# If running inside tmux ($TMUX is set), then change the status line to red
%if #{TMUX}
set -g status-bg red
%endif

# Enable RGB colour if running in xterm(1)
set-option -sa terminal-overrides ",xterm*:Tc"

# Change the default $TERM to tmux-256color
set -g default-terminal "xterm-256color"

# Change the default shell to zsh
if "test -x /bin/zsh" "set -g default-shell /bin/zsh"
if "test -x /usr/bin/zsh" "set -g default-shell /usr/bin/zsh"
if "test -x /usr/local/bin/zsh" "set -g default-shell /usr/local/bin/zsh"
if "test -x $HOME/opt/bin/zsh" "set -g default-shell $HOME/opt/bin/zsh"

# Change the key bindings in copy mode to vi
set -g mode-keys vi

# No bells at all
set -g bell-action none

# Keep windows around after they exit
set -g remain-on-exit on

# Change the prefix key to C-a
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Turn the mouse on, but without copy mode dragging
set -g mouse on
unbind -n MouseDrag1Pane
unbind -Tcopy-mode MouseDrag1Pane

# Some extra key bindings to select higher numbered windows
bind F1 selectw -t:10
bind F2 selectw -t:11
bind F3 selectw -t:12
bind F4 selectw -t:13
bind F5 selectw -t:14
bind F6 selectw -t:15
bind F7 selectw -t:16
bind F8 selectw -t:17
bind F9 selectw -t:18
bind F10 selectw -t:19
bind F11 selectw -t:20
bind F12 selectw -t:21

# Keys to toggle monitoring activity in a window, and synchronize-panes
bind m set monitor-activity
bind y set synchronize-panes\; display 'synchronize-panes #{?synchronize-panes,on,off}'

if 'test $(hostname | grep "mrymtsk")' \
'if "[[ -n $ZDOTDIR ]]" "source $ZDOTDIR/.tmuxline.normal" "source $HOME/.tmuxline.normal"' \
'if "[[ -n $ZDOTDIR ]]" "source $ZDOTDIR/.tmuxline.insert" "source $HOME/.tmuxline.insert"'
bind k confirm-before -p "kill session #S? (y/n)" kill-session
